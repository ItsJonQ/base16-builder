const { baseColors } = require('../colors/solarized')

const baseDark = '002b36'
const baseLight = 'fdf6e3'

module.exports = {
  name: 'Solar',

  vimText: baseLight,
  vimLightText: baseDark,

  baseDark,
  baseLight,

  baseColors,
}
