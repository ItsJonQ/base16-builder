const {
  baseColors,
  baseLightColors
} = require('../colors/subtle')

const baseDark = '002b36'
const baseLight = 'fdf6e3'

module.exports = {
  name: 'Solar',

  cursor: '50E3C2',
  cursorText: '000000',

  vimText: baseLight,
  vimLightText: baseDark,

  baseDark,
  baseLight,

  baseColors,
  baseLightColors
}
